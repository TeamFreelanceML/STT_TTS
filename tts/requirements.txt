# =============================================================================
# requirements.txt — Story Narration TTS API
# =============================================================================
#
# Install (all platforms):
#     pip install -r requirements.txt
#
# System dependency required for soundfile:
#     Ubuntu/Debian : sudo apt install libsndfile1
#     Oracle Linux  : sudo dnf install libsndfile
#     Windows       : bundled automatically via pip
#
# Model files (not installed via pip — place in engines/):
#     engines/kokoro-v1.0.onnx
#     engines/voices-v1.0.bin
#
# =============================================================================


# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0                # Async REST API framework
slowapi>=0.1.9                  # Rate limiting for FastAPI
uvicorn[standard]>=0.29.0       # ASGI server — includes uvloop + httptools


# ── Data Validation ───────────────────────────────────────────────────────────
pydantic>=2.0.0                 # Request/response model validation


# ── Task Queue ────────────────────────────────────────────────────────────────
celery[redis]>=5.3.6            # Distributed task queue for parallel TTS synthesis
kombu>=5.3.4                    # Celery transport layer — message broker abstraction
redis>=5.0.3                    # Redis client — Celery broker + backend + job metadata


# ── Audio Processing ──────────────────────────────────────────────────────────
numpy>=1.26.0                   # Audio sample arrays — concat, silence, dtype casting
soundfile>=0.12.1               # WAV read/write (requires libsndfile system library)
scipy>=1.11.0                   # Pitch shift via resampling — child voice effect


# ── TTS Engine ────────────────────────────────────────────────────────────────
kokoro-onnx>=0.4.0              # Kokoro TTS — ONNX-based voice synthesis engine
onnxruntime>=1.18.0             # ONNX inference runtime (CPU) — used by kokoro-onnx
huggingface_hub                 # Support for automatic model downloads


# ── Linux / Oracle Cloud ARM only ─────────────────────────────────────────────
uvloop>=0.19.0; sys_platform != "win32"     # Faster asyncio event loop (Linux/ARM)
httptools>=0.6.1; sys_platform != "win32"   # Faster HTTP parser for uvicorn


# ── Optional: Monitoring ──────────────────────────────────────────────────────
# Uncomment to enable Celery worker dashboard at http://localhost:5555
# flower>=2.0.1